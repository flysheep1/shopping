# 2026-09-01

## 商城大版本升级（用户确认需求：虚拟商品、无收货地址、后期对接商品 API）

在原登录注册+积分 Demo 基础上完成：

1. **后端模块化重构**：server.js 拆为 `src/db.js`（lowdb+种子商品+迁移）、`src/auth.js`（JWT+限流）、`src/routes/{auth,profile,points,products,cart,orders}.js`。
2. **安全优化**：JWT_SECRET 走环境变量（默认值时告警）；登录统一报"用户名或密码错误"防枚举；注册校验用户名/密码/邮箱格式；uuid 替代 Date.now()；登录/注册内存限流。
3. **积分系统**：签到 +10（后端限每天一次，连续 7 天额外 +20）、每日任务 +50（限一次）、pointsLog 流水表。移除了旧的可刷分接口 `/api/points/add`。
4. **商品/购物车/订单**：种子虚拟商品 6 个；购物车 CRUD（限 10 件/校验库存）；checkout 服务端校验价格库存积分、扣分、减库存、生成兑换码订单。
5. **前端**：新增 Shop/Cart/Orders/PointsLog/Settings 页面；Profile.vue 拆为 ProfileLayout（侧边栏+子路由）+ ProfileHome；顶栏加商城/购物车导航（购物车角标）；401 跳转带 redirect 回跳。
6. 测试通过：签到/任务限次、超量加购拒绝、积分不足结算拒绝、正常结算扣分+发码+库存扣减、流水正确、改密码+新密码登录、统一错误提示。
7. 注意点：vite `base:'/shopping/'`（gh-pages 用），dev 地址是 `http://localhost:5173/shopping/`；本机 vite 只监听 IPv6 `::1`；curl 测试需 `--noproxy '*'`。
